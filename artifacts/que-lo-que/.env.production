# Production environment variables for Capacitor / app-store builds.
# VITE_API_URL is baked into the JS bundle at build time via `vite build --mode production`.

VITE_API_URL=http://187.124.148.162:8080
